#include <stdlib.h>

int **alloc_grid(int width, int height) {
  /* compute number of chars for grid */
  /* compute size of memory to allocate */
  /* allocate memory */
  /* assign chars of grid to memory */
  ;
}
