/*
 * bubble_sort takes in an int array, @array, and bubble-sorts the given array
 * into ascending order.
 */

void bubble_sort(int *array, int size)
{
  int i
  int rounds to sort
  int indices to sort;
  
  for (i = 0; i < rounds; i++, indices--) {
    for (i = 0, i < indices, i++) {
      switch array[i] with array[i + 1] if array[i] > array[i+1];
    }
  }
}
