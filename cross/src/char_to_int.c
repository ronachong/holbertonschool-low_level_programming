/*
 * str_to_int converts the string passed
 * to it to its integer counterpart
 */

int str_to_int(char *s)
{
  int i;
  int i2rtn;
  i2rtn = 0;

  for (i = 0, s[i] != '\0', i++) {
    /* take s[i] and convert to its decimal counterpart */
    /* take the decimal counterpart and convert it to the number of places it should have */
    /* add to i2rtn */
    ;
  }
  return i2rtn
}
